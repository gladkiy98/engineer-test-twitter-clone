<h1>Hashtag!</h1>

<div>
  <div>
    <ul>
      <% @tags.each do |tag| %>
        <li>
          <%= link_to "#{tag.name}", tag_path(tag) %>
        </li>
        <hr>
      <% end %>
    </ul>
  </div>
</div>
